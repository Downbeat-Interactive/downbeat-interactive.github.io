{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Auto Compile JSX",
            "type": "shell",
            "command": "npx babel --watch src --out-dir . --presets react-app/prod",
            "auto": true
        },
        {
            "label": "Auto Compile Blog JSX",
            "type": "shell",
            "command": "npx babel --watch ./blog/src --out-dir ./blog/. --presets react-app/prod",
            "auto": true
        }
    ]
}